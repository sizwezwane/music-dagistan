{
  "name": "music-dagistan",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --outfile=dist/app.js --format=esm --sourcemap",
    "dev": "esbuild src/app.ts --bundle --outfile=dist/app.js --format=esm --sourcemap --servedir=. --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.6.3"
  }
}
